# Get the user's username

Get the user's username

## Usage

``` r
get_system_username()
```

## Value

The username of the user running the script

## Examples

``` r
if (FALSE) { # \dontrun{
get_system_username()
} # }
```
