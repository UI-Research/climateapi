% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_chas_codebook.R
\name{get_chas_codebook}
\alias{get_chas_codebook}
\title{Obtain a codebook for HUD CHAS variables}
\usage{
get_chas_codebook(
  end_year = 2021,
  directory_path = file.path(climateapi::get_box_path(), "built-environment", "hud",
    "comprehensive-housing-affordability-strategies")
)
}
\arguments{
\item{end_year}{The last year of the five-year ACS period, from 2009 to 2021. Defaults
to 2021 (the most recent period with a published data dictionary). If no dictionary
for this period is on disk, the most recent one that is available is used instead
and a warning reports the substitution.}

\item{directory_path}{The directory containing the CHAS data dictionaries. Defaults to
the CHAS folder under the C&C Box path.}
}
\value{
A tibble with one row per CHAS estimate variable and the following columns:
\describe{
\item{\code{column_name}}{The descriptive snake_case name assigned by \code{\link[=get_chas_housing_affordability]{get_chas_housing_affordability()}}.}
\item{\code{column_name_source}}{The source's original variable code, e.g. \code{"T8_est4"}.}
\item{\code{chas_table}}{The CHAS table the variable belongs to, e.g. \code{"T8"}.}
\item{\code{column_type}}{Whether the variable is a \code{"Total"}, a \code{"Subtotal"}, or a \code{"Detail"} line. Totals and subtotals are sums of the lines nested beneath them, so adding across types double counts households.}
\item{\code{definition}}{The variable's full definition as a single sentence.}
\item{\code{vintage}}{The last year of the five-year period whose dictionary these definitions were read from. This equals \code{end_year} unless no dictionary for \code{end_year} was found.}
}
If no usable data dictionary is found, a message is emitted and a zero-row tibble
with these columns is returned.
}
\description{
Reads the CHAS data dictionary that ships alongside the data and returns
it as a tibble: one row per variable, giving the descriptive column name that
\code{\link[=get_chas_housing_affordability]{get_chas_housing_affordability()}} assigns, the source's original variable code, and
a plain-English definition. Use it to find the variables you want before pulling
data, and to interpret the columns you get back.
}
\details{
CHAS variables are organized into numbered tables, each of which
cross-tabulates households by a different combination of characteristics (tenure,
income relative to HUD Area Median Family Income, cost burden, race and ethnicity,
household type, and so on). Each variable's definition is published as a hierarchy of
up to five nested clauses, which are collapsed here into a single sentence
(\code{definition}) and a single snake_case name (\code{column_name}).

Because those clauses repeat the same long phrases in nearly every variable, the
name uses the abbreviations below. \code{definition} always keeps the source's full
wording, so use it whenever a name is unclear.
\describe{
\item{\code{lte}, \code{gt}}{"less than or equal to" and "greater than", as in \code{income_gt_30_lte_50_hamfi} for household income above 30\% and at most 50\% of HAMFI.}
\item{\code{hh}}{household, as in \code{hh_type} and \code{hh_size}.}
\item{\code{unit_problems}}{the source's "4 housing unit problems": cost burden above 30\% of income, more than one person per room, and incomplete kitchen or plumbing facilities.}
\item{\code{severe_unit_problems}}{the source's "4 severe housing unit problems", a stricter version of the same four conditions (cost burden above 50\% of income, more than 1.5 persons per room). This is a distinct measure from \code{unit_problems}, so the two are never collapsed into one name.}
\item{\code{facilities}}{"complete kitchen and plumbing facilities"; \code{lacks_facilities} is the unit lacking them.}
\item{\code{ppr}}{persons per room, the source's overcrowding measure, as in \code{gt_1_ppr}.}
\item{\code{nocalc}}{cost burden not computed, which the source reports for households with zero or negative income.}
\item{\verb{1ormore}, \code{none}}{"has one or more of" and "has none of", used for the housing-problem, age, and disability breakdowns.}
\item{\code{nh}, \code{pi}, \code{aian}}{non-Hispanic, Pacific Islander, and American Indian or Alaska Native.}
}

\code{column_name} is unique within a CHAS table but not across tables -- every table has
its own "owner occupied" total, for example -- so filter to one \code{chas_table} when a
unique key is needed. \code{\link[=get_chas_housing_affordability]{get_chas_housing_affordability()}} handles this by only
renaming columns whose descriptive name is unambiguous among the columns it returns.

Only estimate variables are described. The source's margin-of-error variables
(\verb{_moe} rather than \verb{_est}) share their estimate's definition and so would not have a
distinct name; they are omitted here and keep their original names in the data.

HUD publishes one dictionary per five-year release, and the variable codes change
between releases as tables are added or revised. The \code{vintage} column records which
release the returned definitions come from. When the requested \code{end_year} has no
dictionary on disk, the most recent available release is used as a stand-in; its
definitions are usually still correct, but codes present in one release and not the
other will be missing or mismatched, so check \code{vintage} when the warning appears.
}
\examples{
\dontrun{
codebook = get_chas_codebook(end_year = 2021)

## find the cost-burden subtotals in table 8 (tenure by income by cost burden)
codebook |>
  dplyr::filter(chas_table == "T8", column_type == "Subtotal") |>
  dplyr::filter(stringr::str_detect(definition, "cost burden"))
}
}
