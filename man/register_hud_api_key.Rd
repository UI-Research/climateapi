% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{register_hud_api_key}
\alias{register_hud_api_key}
\title{Register a HUD API key}
\usage{
register_hud_api_key(key, install = FALSE)
}
\arguments{
\item{key}{A HUD API key: a single, non-empty character string. Obtain one by
registering at \url{https://www.huduser.gov/portal/dataset/fmr-api.html}.}

\item{install}{Logical. If \code{TRUE}, write the key to your user-level \code{.Renviron}
(replacing any existing \code{HUD_API_KEY} entry) so it persists across sessions.
Defaults to \code{FALSE}, which sets the key for the current session only.}
}
\value{
Invisibly returns \code{key}. Called for the side effect of setting the
\code{HUD_API_KEY} environment variable (and, when \code{install = TRUE}, writing it to
\code{.Renviron}).
}
\description{
Stores a HUD API key so that \code{\link[=get_chas_housing_affordability]{get_chas_housing_affordability()}} can authenticate against
HUD's CHAS API. By default the key is set for the current R session only; set
\code{install = TRUE} to also persist it to your user-level \code{.Renviron} so that it is
available in future sessions.
}
\examples{
\dontrun{
register_hud_api_key("your-hud-api-key")
register_hud_api_key("your-hud-api-key", install = TRUE)
}
}
